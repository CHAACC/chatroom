export const SERVER_URL =
    window.location.hostname === 'localhost'
        ? `//${window.location.hostname}:7001`
        : `//${window.location.hostname}`

export const QN_DOMAIN = '//img.yaojiang.top'
